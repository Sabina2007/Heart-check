from instr import *
class MainWin(QWidget):
    def initUI(QWidget):
        self.hello_text = QLable(text_hello)
        self.instruction = QLable(text_instruction)
        self.button = QPushButton(text_nextp)
